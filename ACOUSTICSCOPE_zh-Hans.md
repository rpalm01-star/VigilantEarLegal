# Acoustic Scope — 专业声音分析视图

**Acoustic Scope** 把 Vigilant Ear 变成一台装在口袋里的声音分析仪：用六种不同方式实时呈现麦克风听到的一切。用它来 *看见* 一个声音的形状、测量它的音高与电平、冻结并回溯最近一分钟，并截取片段来训练你自己的自定义声音包。

从 **动作扇面**（顶栏上旋转的光束）打开它：点按光束，再点按 **均衡器**（绿色与青色的动态条）。Acoustic Scope **对所有人免费**，下面的实时视图无需任何购买。只有 **Train** 采集工具（见下文）属于 Power Pack+，它用来为你的自定义声音包录制片段。

---

## 顶栏

- **dB 框** — 实时宽带电平。**A / C / Z** 方块选择频率计权（A ≈ 人耳听起来有多响；C 保留更多低频；Z 为平直/不计权）。该计权同样驱动 ⅓ 倍频程视图。
- **root**（仅在色度视图中）— 房间里最强的音高类别，实时更新。
- **✕** 关闭示波视图。只要它处于打开状态，检测与警报始终继续运行：它是一个窗口，而不是一种模式。

## 六种视图

用底部的条切换。

| 视图 | 显示什么 |
|---|---|
| **频谱** | 此刻按频率分布的电平，一条带白色峰值保持线的实时曲线。 |
| **声谱图** | 频率随 **时间** 变化，最近约一分钟滚动而过，颜色代表电平。多数声音在这里都有可辨认的视觉形状。 |
| **⅓ 倍频程 RTA** | 28 个标准 ISO 频带，如同硬件实时分析仪。橙色刻度是峰值保持。 |
| **色度** | 12 个音高类别，显示当前出现了哪些音，并突出最强的一个。 |
| **泛音** | 把突出的纯音随时间追踪为彩色线条，每条标注其音名。音名与泛音列各有独立的一栏，烟雾报警器这类较高的声音也会按真实音高命名。选择一个或多个 **目标音**（可含升号、降号和八度），每个都会显示为一条线，供你对照着演唱或调音，视图也会缩放到它们所在的音域。非常适合口哨、警笛、鸟鸣和机器嗡鸣。 |
| **可视化器** | 把房间里的音乐化作实时灯光秀：节拍引爆光环，低音让画面鼓胀，高音洒下火花。下文另有专节。 |

声谱图上的橙色 **旗标** 标记声音分类器触发的时刻，并带上它的标签与置信度，让你确切看到模型对什么形状做出了反应。

## 读数与测量

- **左侧滑块** — 显示比例。把爆表的视图拉低，或把安静的视图抬高（仅影响显示，绝不影响检测）。
- **点按** 声谱图，得到十字线读数：频率、电平，以及距今多久。
- **拖出** 一个方框，得到该区域的统计：频率跨度、时长、峰值、质心、能量、峰值因数。
- **捏合** — 缩放频率轴。⤢ 按钮可复位。
- **ⓘ** — 遥测面板（FFT 细节、主导频率、谱质心），以及一个 **校准** 步进器；若你曾与基准声级计比对过，它会整体平移所有电平读数。

## 冻结与回看

**暂停** 按钮冻结画面（麦克风与警报继续运行）。冻结时会出现一条传送条：

- **▶** 以视觉方式回放缓冲区；速度按钮在 1× / 2× / 0.5× 之间循环。
- **🔍− / 🔍+** 缩放时间窗口；右侧的 **缩略图** 显示整个缓冲区，拖动它即可擦洗定位。
- 声谱图与泛音共用同一个时钟，因此你可以在完全相同的冻结时刻在两者之间切换。

## 为自定义声音包采集声音

这正是示波视图的独门本领：在你 *听到声音的当下*，直接从实时视图里取到它的真实样本。

1. 点按品红色的 **Train** 按钮（虚线方块）。视图冻结，并出现一条品红色的 **选取带**。
2. 拖动选取带的两端，框住该声音的一个干净样本，标签会显示所选时长。声音前后各留一两秒最为理想。
3. 点按 **存储**（箭头入托盘按钮）。选取带下方的音频会写入一个片段，并出现一个编号方块。选取带仍处于待命状态，你可以擦洗到下一个样本再次存储（每次会话最多 6 个）。连点编号方块两次可删除该片段。
4. 点按 **锤子** 打开 **Build** 面板：
   - **模型名称** — 声音包的名称（例如 *后院的猫头鹰*）。
   - **声音名称** — 用户在地图上看到的名称。
   - **类别标签** — 训练好的模型将输出的确切标签（自动推导，小写并使用下划线）。
   - **检测到时** — *仅地图*（一个点，用于识别）或 *移动中*（像车辆一样被追踪）。自定义声音只做识别，不会发出紧急警报，那始终由内置的安全检测负责。
   - 图标、颜色、置信度阈值与最大距离；实时预览卡片会准确呈现一次检测的样子。
5. 点按 **Build & Export**。你会得到一个 zip，内含你的片段（已按 Create ML 的文件夹结构组织）以及声音包文件，可直接分享到 Mac。
6. 在 Mac 上，用 `clips/` 文件夹在 Create ML 中训练一个 **Sound Classifier**，把导出的 `model.mlpackage` 放进声音包文件夹，重新压缩，然后在手机上通过 **警报来源 → 自定义声音包** 导入。

训练与导入这一半 — 包括 **必需的 Background 类别** 以及防止误报的把关机制 — 在 **[自定义声音包指南](https://vigilantear.com/zh-Hans/byom/)** 中有逐步说明。

## 可视化器标签页 — 化作光的音乐

**可视化器** 标签页把房间里的音乐变成一场灯光秀，
让失聪、听力障碍或 CODA 观众能够 *用眼睛感受*。其中没有任何装饰性元素，
每一样都由来自麦克风的真实声学特征实时驱动：

- **每一拍引爆一个光环** — 起音检测器触发光环爆裂与屏幕闪光，
  时机恰好是听人感受到底鼓的那一刻。
- **低音在呼吸** — 中央的锚定光环与漂移的纹理光环，
  都会随低频能量鼓胀。
- **高音洒下火花** — 镲与踩镲化作明亮的水滴落下。
- **歌名绕着地球仪** — 歌曲被识别后，标题会环绕一颗掠过画面的隐形地球仪的赤道，
  艺人名则占据右上角。

在 **偏好设置 → Acoustic Visualizer** 中设置你的 **DJ 名称**（及其颜色），
它占据左上角，样式与艺人标签一致。

**投到电视上：** 点按示波视图顶栏的 **tv 按钮**，用 USB-C–HDMI
线缆或 AirPlay 屏幕镜像连接，然后按下 **Mirror**。大屏幕只显示图形，
而这部手机仍是控制器与麦克风。在手机上切换示波视图的标签页，
电视会随之跟进，因此同一个 Mirror 既能把声谱图投上墙，也能把可视化器投上墙。

## 值得了解

- 关闭时示波视图不产生任何开销，额外分析只在它显示于屏幕时运行。
- 让它一直开着，开销也很小：声谱图几乎不耗资源，无论你看多久都不会让手机发热；声谱图与泛音会以稳定、易读的节奏刷新，同时不会漏掉它们听到的任何声音。
- 电平开箱即已校准：我们在 iPhone 16 Pro Max 上将其与基准声级计（A 计权）对齐到约 ±1 dB 以内，iPhone 17 与之相差不到 0.5 dB。其他机型的读数可能略有不同；这是我们自己的校准，并非认证测量——如果你有基准声级计，ⓘ 中的校准步进器可以微调所有电平读数。1.1.9 版之前设置的校准偏移已重置为零，因为它所修正的默认值已经改变。
- 示波视图读取主麦克风通道。你在这里所做的一切都不会影响检测、测向与警报。

---

<p align="center">
  <img src="https://raw.githubusercontent.com/rpalm01-star/VigilantEarLegal/main/wingdings-logo.png" alt="Wingdings, Inc." width="102" /><br /><br />
  <strong>© 2026 Wingdings, Inc.</strong><br />
  All rights reserved.<br />
  Patent Pending
</p>
